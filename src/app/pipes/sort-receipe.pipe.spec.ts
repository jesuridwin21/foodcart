import { SortReceipePipe } from './sort-receipe.pipe';

describe('SortReceipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortReceipePipe();
    expect(pipe).toBeTruthy();
  });
});
